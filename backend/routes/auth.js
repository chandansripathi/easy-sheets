// GitHub Copilot Prompt:
// Implement simple login/logout routes using JWT.
// POST /api/login -> verify username/password, return token.
// Protect admin routes with middleware checking JWT role = "admin".
